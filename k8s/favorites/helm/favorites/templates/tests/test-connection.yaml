apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "favorites.fullname" . }}-test-connection"
  labels:
{{ include "favorites.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "favorites.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
