server:
  port: 8092

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost/favorites?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    io.favorites: info

